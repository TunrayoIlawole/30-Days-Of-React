1. Why do we need to map an array?
We need to map an array in order for it to appear in a desired form on the browser by using html elements.

2. Why do we need keys during mapping an array?
Keys help React to identify items that have been changed, removed or added. Keys give elements in an array a stable identity.

3. What is the importance of destructing your code?
It makes access to elements and values in arrays and objects easier.

4. Does destructuring make your code clean and easy to read?
Yes, it does.